{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "crds": {
      "type": "object",
      "properties": {
        "install": {
          "type": "boolean",
          "description": "Whether to install/upgrade CRDs from templates/crds/. Set to false to manage CRDs externally."
        }
      },
      "additionalProperties": false
    },
    "networkPolicy": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable NetworkPolicy for the strimzi operator pod."
        },
        "flavor": {
          "type": "string",
          "enum": ["cilium", "kubernetes"],
          "description": "NetworkPolicy flavor: 'cilium' for CiliumNetworkPolicy, 'kubernetes' for standard NetworkPolicy."
        }
      },
      "additionalProperties": false
    },
    "kyvernoPolicyExceptions": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether to create a KyvernoPolicyException for the operator."
        },
        "namespace": {
          "type": "string",
          "description": "Namespace where KyvernoPolicyExceptions are created (must match Kyverno's policyExceptionNamespace setting)."
        }
      },
      "additionalProperties": false
    },
    "verticalPodAutoscaler": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Create a VerticalPodAutoscaler for the operator. Requires VPA CRDs on the cluster."
        },
        "updateMode": {
          "type": "string",
          "enum": ["Auto", "Recreate", "Initial", "Off"],
          "description": "VPA update mode: Auto/Recreate restart pods to apply recommendations; Initial applies only at creation; Off disables updates."
        }
      },
      "additionalProperties": false
    },
    "podMonitor": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Create a PodMonitor to scrape operator metrics on port 8080."
        },
        "labels": {
          "type": "object",
          "description": "Labels added to the PodMonitor resource. Use 'observability.giantswarm.io/tenant' to route metrics to a Grafana Organization.",
          "additionalProperties": { "type": "string" }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true
}
